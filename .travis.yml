slanguage: python
python:
  - "3.5"

install:
  - python3.5 -m pip install pyzmq
  - python3.5 -m pip install coverage coveralls


# command to run tests
script:
  - coverage run --source=hermes setup.py test
  - coveralls

  - python3.5 tests.py



branches:
  only:
  - master
