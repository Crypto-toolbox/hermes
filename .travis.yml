slanguage: python
python:
  - "3.5"

install:
  - pip install pyzmq
  - pip install coverage coveralls


# command to run tests
script:
  - coverage run --source=hermes setup.py test
  - coveralls

  - python tests.py



branches:
  only:
  - master
